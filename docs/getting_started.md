# Getting Started

## Installation

1. `pip install -e .`

## Explore Reactors

The `docs/reactor_browser.ipynb` notebook lets you compare scenarios in `reactors/`.

1. `python -m jupyter lab` (or `python -m jupyter notebook`)
2. Open `docs/reactor_browser.ipynb`
3. Run the cells to browse, filter, and compare reactor data
